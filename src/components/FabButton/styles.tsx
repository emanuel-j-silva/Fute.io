import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container:{
        position: "absolute",
        bottom: 10,
        right: 15,
    },
    button:{
        padding: 35,
        borderRadius: 50,
        opacity: 0.9
    }
});

export default styles;